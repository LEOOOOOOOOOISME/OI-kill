{
  "name": "oikill-v4-web",
  "version": "4.0.0",
  "private": true,
  "description": "《OI杀》v4.0 双版本(单人/局域网) —— 引擎/网络/UI/AI 工作区(P5 打包)",
  "main": "game.js",
  "scripts": {
    "build:pages": "node build/bundle-pages.mjs",
    "build:server": "node build/bundle-server.mjs",
    "e2e:single": "node build/single-e2e.mjs",
    "build": "npm run build:pages && npm run build:server"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.28.2",
    "postject": "^1.0.0-alpha.6"
  }
}
