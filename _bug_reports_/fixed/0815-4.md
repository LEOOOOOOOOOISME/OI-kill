# 0815-4 [fixed] UI 第十七章：牌堆计数 / 弃牌堆查看 / 响应倒计时

> 需求 17.7 要求：牌堆计数（"🃏 余 41 张"）、弃牌堆可点击展开查看、响应限时提示（10 秒超时视为不打出）。
> 本批补齐这三项；其余 UI 项（五状态反馈链/特效强度三档/"你可以…"提示/模态框/新手教程）留待后续。

## 服务端 (game_engine.cpp getStateJson)
- 新增 `deck_count` / `discard_count`：牌堆与弃牌堆张数（全员可见）
- 新增 `discard`：弃牌堆全部牌面（需求 1.2 弃牌堆"正面朝上，公开"）
- 新增 `pending_timeout`：当前待响应的剩余秒数（仅发给响应者；超时由 resolvePendingTimeout 按放弃处理）

## 前端 (html_content.h)
- 手牌区上方新增牌堆栏：`🃏 牌堆余 N 张 · 弃牌堆 M 张 [🗑️ 查看弃牌堆]`
- 新增弃牌堆弹层：点击按钮弹出全屏遮罩，展示弃牌堆全部牌面（花色+点数+卡名），可关闭
- 响应倒计时：待响应提示下方显示 `⏳ 限时 N 秒`，每秒刷新，≤5 秒变红提示"即将超时"

## 验证
- build.bat BUILD OK；OIKillServer.exe 启动 /login HTTP 200
