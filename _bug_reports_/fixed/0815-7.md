# 0815-7 [fixed] UI 第十七章: 五状态反馈链 / 特效强度三档 / "你可以…"提示 / 模态框 / 新手教程

## 1. 五状态反馈链 (需求17.7)
- 选中态: 已有(选中上浮+金框+注释气泡) ✓
- 目标态(新增): 选中攻击牌后, 服务器下发 `can_attack_targets`(getStateJson),
  可攻击目标座位加绿色呼吸光圈(tgt), 非法目标灰暗不可点(nontgt); 锦囊类对所有其他存活玩家亮光圈(服务端校验)
- 确认态(新增): 点击目标后弹出确认条 "对 N 号 使用【X】? [✅确认][✖取消]",
  覆盖单目标/找代打两目标/多目标(放手一搏·AK全场); 点击自己或取消可随时取消 (17.7 可取消性)
- 执行态/结果态: 已有 flashSlot 受击/治疗高亮 + fx 结果横幅 + 日志滚动 ✓

## 2. 特效强度三档 (需求17.9)
- 顶栏新增按钮 `✨ 特效` 循环切换 全效/轻量/无, localStorage 持久化
- 全效: 全部动效+音效; 轻量: 仅 A 级(觉醒/进化/阵亡/AOE/事件/胜利)保留, B/C 级静默;
  无: 全部禁用(仅文字日志)
- fx()/snd() 增加级别过滤; 日志触发块 A 级事件标记

## 3. "你可以…"提示 (需求17.10)
- 出牌阶段提示区显示合法操作清单: 攻击牌带目标号(如"使用【做法假了】攻击 2号/5号"),
  装备/自动牌/锦囊分别生成条目, 最多6条+省略

## 4. 模态框 (需求17.11)
- 待响应时(WA/特判/进化选择/AOE/对拍/模拟赛等全部 pending)弹出全屏模态遮罩
  (`#modal-mask` + 居中 `#prompt`), 遮罩不响应点击, 禁止误点其他区域
- 暖色主题下遮罩适配暖棕

## 5. 新手教程 (需求17.10)
- 首次进入游戏界面弹出3步教程(如何出牌/如何响应/如何弃牌), 支持 上一步/下一步/跳过,
  localStorage 记录已读, 后续不再弹出

## 验证
- build.bat BUILD OK (exit 0, 无警告)
- 前端 JS 经 node --check 语法检查通过 (从 GAME_HTML 提取 <script> 校验)
- OIKillServer.exe 启动 /login HTTP 200
