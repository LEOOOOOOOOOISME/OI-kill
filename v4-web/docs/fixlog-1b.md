# Fix-1b 修复日志（v4-web/game.js 引擎）

- 修复代理：Fix-1b
- 范围：仅 `v4-web/game.js`（未触碰 index.html / test.js / requirement.txt）
- 输入：`docs/fixlog-1a.md`（已读，未回退任何 1a 修复）、`docs/recon-04-审计B-规则逻辑.md`、`requirement.txt`
- 结论：recon-04 的 M 级清单全部处理（含已知清单 7 项 + 其余 M 项 6 项），L 级 10 项修复/决策，需复核 12 项逐项评估；`node --check` 通过；定向验证 41 项全过；测试 **A 40/40、B 5/5、C 6/6**（连续 5 轮稳定）

---

## A. M 级项目（按 recon-04）

### M-2 三个职业技能整体缺失：评测姬·测评 / 传奇Au·不败 / 划水怪·随缘 ✅已实现

- 需求：req 13 职业表 L521「评测姬…测评(被动)：受伤判定红桃伤害-1…觉醒：受伤减伤免判定（每回合1次）」；L525「传奇Au选手…不败(被动)：每回合1次卡牌效果伤害可防止」；L522「划水怪…随缘(被动)：摸牌阶段可改为摸1+弃牌堆拿1张基本牌」。
- 改动：
  - `loseHp`（L546，测评块 L595–607）：基础测评每次受伤判定，红桃伤害 -1；觉醒后免判定、每回合 1 次减 1（沿用 `armorCount.pingce`，startTurn 已重置）。
  - `loseHp`（不败块 L618–626）：`chuangqi` 且来源为卡牌效果（attack/pierce/o2/duel/aoe/argue/ub）时，每回合 1 次防止（`armorCount.bubai`）；技能伤害（封神/爆零/挣扎）与事件（过载/梅花自伤）不触发——符合「卡牌效果」限定。
  - `drawPhase`（L415–431）：`huashui` 玩家在弃牌堆有基本牌时自动改为「摸 1 + 拿回 1 张基本牌」（引擎择优，无挂起询问），否则照常摸 2。
- 说明：三项均为被动技，不进入 `SKILLS` 表（该表只列主动技），故 SKILLS/skillUse 无对应条目；任务书中「implement in SKILLS + skillUse」按被动技的正确落点（loseHp/drawPhase，与 recon M-2 修复建议一致）实现。
- 验证：定向脚本——红桃判定 -1、觉醒免判定且每回合 1 次、不败首伤防止/次伤生效、随缘摸 1+拿基本牌，全过。

### M-3 特判系统缩水：范围不全 + 不可连锁 ✅已实现

- 需求：9.2 L370「特判…抵消一张正在结算的锦囊对一名角色的效果（AOE 中可对单名受影响角色使用）；**可连锁反制特判**。不能抵消延时锦囊、装备与技能」；FAQ#2 L632「特判可反制特判」。
- 改动（特判体系重构，L1107–1287）：
  - `COUNTERABLE`（L1109）扩为全部非延时锦囊 + counter/counterEvo（原仅 14 种）。
  - 新增 `counterChain`（L1115）：从上一张特判使用者下家起按座位序询问连锁反制；链上**奇数张特判生效**（原锦囊被抵消），偶数张使前一张失效（原锦囊继续结算）；人类挂起 `type:'counter'` + `ctx.type:'counterChain'`（**复用既有 pending 类型与 respondCounter，不新增导出键**）。
  - `tryCounter`（L1138）接受 `cont`（继续信息）并在 AI 使用特判后进入连锁；新增 `tryCounterOther`（L1158）供「其他玩家反制自益锦囊」（摸鱼/骗分/申诉/复盘/小抄/躺赢/摆烂/开小号/题解大会选牌段）、`counterAsk`（L1181）统一询问入口、`runCounterCont`（L1199）链冻结收尾、`contFromCtx` 由挂起 ctx 重建继续信息。
  - 新增 `doTrickSelf`（L1289，自益锦囊核心效果）、`doKillUnit`（L1343，删库+亡语+进化）、`powerStep`（L1354，机房断电按单位主人分段反制）。
  - `playCard` 各锦囊 case 全部接入特判（单目标 trick / AOE 每段 / allHeal / funCcf / harvest 每名选牌者 / killUnit / funPower 每个单位主人）；`resumeAoe`/`argueStep`/`harvestStep` 同步改用 `counterAsk`。
  - 边界决策：**卖队友（funBetray）** 为响应轨锦囊，其「被反制的角色」不明确（转嫁的受益/受害方难定），未接入特判，保留现状并记录（见需复核补充）。
- 已知小偏差：连锁中被反制的特判仍会计入「一票否决」进化候选（使用即登记，未区分最终生效与否；需 2 张以上特判才可能出现，属低概率边缘，未再引入延迟结算）。
- 验证：定向脚本——摸鱼被 AI 特判抵消、B 特判→C 连锁反制→原锦囊继续结算、人类挂起 counterChain 并作答、C5/C6 既有单测保持绿色。

### M-4 评测机事件对 AOE 伤害无效 ✅已修复（±1 部分）

- 需求：17 章 L596–599 事件效果；L601「事件同样作用于 AOE 伤害」；FAQ L363「AOE 不吃技能/武器/咖啡加成（事件修正除外）」。
- 改动：`aoeApplyOne`（L1468）两处 `loseHp` 改传 `effDmg`（原计算 effDmg 后仍用原始 dmg，为死计算）；AOE 伤害现随事件 黑桃 -1（最低 1）/ 梅花 +1；梅花事件下防火墙本就不免疫（既有逻辑 `eventSuit !== 'club'`）且 +1 生效。
- 待裁决：梅花事件「命中后使用者自己受 1 伤」是否逐目标作用于 AOE 使用者——17 章自伤条款只写在「做法假了」上，recon M-4 亦要求「明确规则后补实现」，本次仅落实 ±1，自伤部分记为【歧义待裁决】。
- 验证：定向脚本——club 事件下 aoeAtk 两名目标各受 2 伤。

### M-5 亡语触发缺失（删库 / 机房断电）✅已修复

- 需求：10.1 L428「亡语＝被消灭时原主人摸 1 张」；10.3④ L457「单位直接阵亡（可触发亡语）」；9.2 L376 删库、18.3 L623 机房断电。
- 改动：
  - `unitDie`（L2348）收紧：**克隆体（id=-1）不入弃牌堆且不触发亡语**（防牌张虚增、守恒破坏；顺带修复 unitAttack 杀克隆亡语单位的潜在守恒漏洞）。
  - `doKillUnit`（L1343，playCard killUnit/killUnitEvo 路径 L1810–1818）：消灭单位后调 `unitDie(g, target.id, u, 'killUnit')`，原主人亡语摸 1；进化候选/进化摸 1 逻辑保留（未动 1a 的进化合并）。
  - `powerStep`（L1354，funPower 路径 L1919–1923）：每消灭一个单位调 `unitDie(g, q.id, u, 'funPower')`（owner 为对方）。
- 验证：定向脚本——删库/机房断电消灭亡语单位原主人各摸 1；克隆亡语单位被消灭不摸牌。

### M-7 主公首轮免伤/护盾/挡刀被 ub/struggle 绕过 ✅已修复

- 需求：2.6① L136「首轮免疫…覆盖所有来源：做法假了/锦囊/事件/技能」；FAQ#6 L636「首次伤害（任何来源，含锦囊/事件）免疫」；L137/L419 护盾「每回合免疫首次伤害」；L143 忠臣挡刀无来源限制。
- 改动：`loseHp` bypass 白名单（L550）由 `['struggle','overload','ub','guard']` 收窄为 `['overload','guard']`——UB（锦囊）与挣扎（技能）现在完整经过首轮免伤/护盾/挡刀链。保留：`overload`（题海战术全局惩罚，recon 裁定保留待需求明确）、`guard`（挡刀递归防环）。
- 验证：定向脚本——主公首轮 UB 被免伤、挣扎被护盾吸收、两者耗尽后 UB 实际结算。

### M-8 forceEndByCount 主公方计数拆分错误 ✅已修复

- 需求：2.8 L165 / FAQ#15 L645「存活人数多者胜；人数相同则内奸（若存活）单独获胜」。
- 改动：`forceEndByCount`（L722）改为按阵营合并计数：主公方 = lord+loyal 合并；人数最多且唯一 → 该阵营胜（不再出现 lord+loyal+traitor 误判 1:1:1）；并列且内奸存活 → 内奸单独获胜；并列且无内奸 → 需求未定义，记平局并按主公方结算（保留现状兜底，日志注明）。
- 验证：定向脚本——存活主公方 2 人 vs 内奸 1 人判主公方胜；主公方 1 vs 反贼 1 vs 内奸 1 判内奸单独胜。

### M-1 多职业觉醒升级未实现 ✅已实现（五职业）

- 需求：req 13 觉醒列 L519（卡常不再消耗手牌）、L520（直播可看 2 张拿 1 张）、L530（口嗨可指定两名玩家）、L531（抄题解看 4 取 2）、L533（水群摸 3 弃 1）。
- 改动：`skillUse`（L2375）各分支读 `p.awaken`：
  - `kachang`：觉醒免弃手牌（0 手牌可发动）；
  - `live`：觉醒随机看目标 2 张手牌、随机拿走 1 张、另一张放回（引擎自动，无新 pending）；
  - `kouhai`：觉醒作用于两名玩家；`skillUse` 扩展可选第 5 参 `targetId2`（向后兼容），未传时自动选另一目标；
  - `chao`：觉醒看 4 取 2（取牌堆顶 2 张，余牌按序放回牌堆顶，守恒）；
  - `shuiqun`：觉醒摸 3 弃 1。
  - `aiSkill` 毒瘤门槛放宽：觉醒后无手牌也可发动卡常。
- 说明：人类取牌选择的 UI 交互未新增挂起（54 键导出约束，且需求正文未要求交互形态），引擎自动裁决；对应 recon L-10 一并处理。
- 验证：定向脚本——0 手牌卡常、口嗨双目标各弃 1、抄题解 4 取 2（净 +1、牌堆 -2）、水群净 +1，全过。

### 其余 M 项（不在已知清单、recon M 级，一并修复）

- **M-9 事件翻牌掏空牌堆静默洗回** ✅：`endTurn`（L447–521）翻事件牌时牌堆空 → 统一走题海战术（洗回+全体-1+`reshuffleCount++`+连续两次保底终局），翻出最后一张后牌堆空同样处理；消除两处静默洗回导致的计数失真。
- **M-11 护驾·玄学判题免费抵消漏洞** ✅：`helperDodge`（L2205）签名扩展 `(g, helper, attacker, lord, dmg, opts)`：先做玄学判题（红桃→免费代出 WA，管理员权限无视），再尝试 WA/并查集；全部失败时改 `resolveHit` 命中主公（不再无判定免费抵消）。`attackPlayer`/`respondDodge` 调用点同步传 dmg/opts。
- **M-12 管理员权限不能无视 AOE 防火墙** ✅：`aoeApplyOne`（L1497）与 `respondAoeResp`（L1605）防火墙免疫条件加 `!(src.weapon.key==='wQgj')`。
- **M-13 AC保护离场回血未实现** ✅：`unequipArmor`（L2331）新增第 4 参 `silent` 与 aAc 回血（失去防具 +1，濒死/阵亡不回）；调用点区分：换装（equipCard、抄袭缴获中的顶替）silent=true 不触发（铁律 2），缴获/拔网线（非换装失去）触发回血，退役救援链 silent（统一回 1）。
- **M-14 奠基（图灵奖得主）提前触发觉醒** ✅：`dianji`（L2491）仅置 `usedDianji`（手牌上限 +2 由既有公式读取），移除 `p.awaken=true` 与摸 2；觉醒（摸 2）回归体力阈值触发（16.1）。
- **M-15 平衡树强制命中固定 1 点伤害** ✅：`afterDodge`（L993）新增 dmg 参数，`resolveDodge`/`helperDodge`/`respondDodge` 全程贯通原攻击最终伤害；bbst pending 与强制命中均用该 dmg（`respondBbst` 读 `pd.dmg` 无需改）。

---

## B. L 级项目

- **L-1 搅局者缺「未获胜」** ✅：`settleAchievements`（L771）加 `g.winner !== '内奸(摸鱼怪)'`。依据 req 2.9 L173「未获胜但存活至终局」。
- **L-2 卖队友需同意**：Fix-1a 第 8 项已完成，跳过。
- **L-3 明君标记时机** ✅：`loseHp`（L631）标记移到实际扣血后；防具减免至 0 不再误记「对忠臣造成伤害」。
- **L-4 萌新问问题在 AOE 触发** ✅：`aoeApplyOne`/`argueApplyOne` 移除 `onBecomeTarget`（AOE/双目标非「唯一目标」，req 13 L527）。
- **L-5 女装大佬需求外被动** ✅：`onBecomeTarget`（L2233）删除「成为锦囊目标摸 1」分支（req 13 无此技能）。
- **L-6 忠臣挡刀无玩家选择**：【保留现状】——引擎自动挑第一个有手牌忠臣弃牌挡刀；人类忠臣的选牌/确认属 UI 层交互，`respondGuard` 为兼容占位 API，不影响规则正确性（挡刀效果本身已实现）。
- **L-7 题解大会选牌顺序** ✅：`playCard` harvest（L1863）顺序改为从使用者本人起（req 9.2 L358「从你起按行动顺序各选 1 张」）。
- **L-8 UB 传给已有 UB 的下家堆叠** ✅：`judgePhase`（L405）循环跳过判定区已有 UB 的玩家（req 9.2 L361；牌堆仅 1 张 UB，此路径属防御性修复）。
- **L-9 并查集免费挡的限制** ✅：`resolveDodge` 条件放宽为「无 WA **或灵感不足**」时弃 1 手牌抵挡（req 9.3 L412「被攻击时可弃 1 张手牌视为打出 WA（免灵感）」）。
- **L-10 女装直播/抄题解取牌无人类选择**：【保留现状】——引擎自动取牌（直播觉醒随机 2 取 1、抄题解取顶 2），人类选牌交互受 54 键导出约束暂不新增挂起；随 M-1 已实现效果本身。
- **L-11 神犇碾压排除黑色响应牌**：归入需复核 #8，【保留现状】。
- **L-12 内奸离场 UI 身份隐藏** ✅：`playerLeave`（L2597）置 `p.left`，`publicView` others 身份逻辑改为「死亡内奸仅在未离场时隐藏」（req 2.4 L118–120 例外二）。

---

## C. 【需复核】清单评估（recon-04 12 项）

1. **MLE 锦囊**：Fix-1a 第 7 项已清理 requirement.txt 两处引用，game.js 全文无 MLE——【已由 1a 解决】。
2. **O3优化进化**：req 9.2 O2 行残留「进化候选【O3优化】」但 15.2 进化总表（权威）无此项——【保留现状】，总表为准，代码不新增。
3. **卖队友/开小号「一局一次」语义**：两牌牌堆各 1 张，全局标记与「每玩家一次」无实际差异——【保留现状】。
4. **抄袭代码缴获主公护盾（aShield）**：需求「可被拆」未禁缴获；假牌 id=-1 转移不影响守恒——【保留现状】。
5. **金牌教练谈心能否自救**：12.1 文字含糊（「场上有存活金牌教练」是否含自身）——【保留现状】排除自身。
6. **备用电源 vs 咖啡濒死顺序**：需求未定义——【保留现状】（备用电源先于咖啡）。
7. **对拍谁先出攻击**：需求未定义——【保留现状】（使用者先出）。
8. **神犇碾压能否用黑色响应牌**：recon 正文「确认」节已背书现状（非响应牌、黑色），与 L-11 自相矛盾——【歧义待裁决】，保留现状。
9. **判定空仓洗回不触发过载**：判定专用洗回属合理设计（recon 已注明）——【保留现状】。
10. **主人阵亡单位入弃牌堆不触发亡语**：阵亡弃置 ≠「被消灭」，且主人已死摸牌无意义——【保留现状】。
11. **黑名单默认花色为黑**：间接攻击从严判黑——【保留现状】。
12. **反贼首轮 +1 费与感谢CCF +1 费叠加**：需求未禁止——【保留现状】。

补充（本次新增待裁决）：卖队友（响应轨）的特判可反制边界、梅花事件 AOE 使用者自伤、连锁中被反制特判的进化候选登记时点。

---

## 约束核对

- DECK_COUNT 保持 120，未增删任何入牌堆牌种；假牌（id=-1）守恒排除逻辑未动（unitDie 收紧后克隆亡语不再产生真牌）。
- module.exports 保持 **54 键**（实测 `Object.keys(API).length === 54`）；新增函数（counterChain/counterAsk/tryCounterOther/contFromCtx/runCounterCont/doTrickSelf/doKillUnit/powerStep）均为内部函数。
- 未触碰 index.html / test.js / requirement.txt；Node（CommonJS）+ 浏览器双兼容（无新语法，未用对象展开/可选链）。
- publicView.pending 的 srcId/dmg 契约（Fix-1a H-3）未动；连锁询问复用 `type:'counter'` + ctx 扩展，不引入新 pending 类型（B 套件解析器零改动）。

## 测试结论

- `node --check game.js`：通过。
- 定向验证（临时脚本，验证后已删除）：M-2/M-3/M-4/M-5/M-7/M-8/M-1/M-11/M-12/M-13/M-14/M-15 + L-1/L-4/L-7/L-8/L-9/L-12 共 41 项断言全部通过。
- `node test.js`（v4-web 目录）连续 5 轮：
  - **A：40/40**（含 120 守恒 40/40；基线 39/40 的 seed=29 hand-limit 容差断言因轨迹变化不再触发——该断言为测试自身容差启发式，属另一代理处理范围；本批修复未使任何种子退化）
  - **B：5/5**（Math.random 非确定，5 轮稳定）
  - **C：6/6**
- 牌种审计仅存进化改写噪声（attack/dodge/counter 等负向 = 进化改名），无新增增生项。

## 最终结果行

```
=== 汇总: A通过=40/40 (不含120守恒=40/40) | B通过=5/5 | C通过=6/6 | 耗时=0.1s ===
```
