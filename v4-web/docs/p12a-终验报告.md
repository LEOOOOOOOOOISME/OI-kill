# P12a 终验报告（OI杀 v4 本地终验与交付准备）

> 执行代理：P12a（final-local-preparation）
> 环境：Node v24.18.1、npm 11.16.0、esbuild + postject@1.0.0-alpha.6（已装，无网络操作）
> 结论：**全部任务完成，全绿。** 双 exe 重建并冒烟通过（核心证据：`src/engine/core.js` 服务内容与磁盘逐字节相等且含新平衡规则标记）、pages 单文件重建并 vm 冒烟 14/14、两个发布目录全量刷新、更新日志 v4.0 完成节写入、终验门禁 9 项全绿、feat 提交 `70f9d0f`（未 push）。
> 改动范围：仅 `v4-web\build\`（新增 2 个烟测脚本 + 再生 3 个构建产物）、`v4-web\dist\`（重建 6 个产物）、`GithubServerVer\`、`UserVer\`、`CHANGELOG.md`、`更新日志.md`、`v4-web\docs\p12a-终验报告.md`。**未触碰** `v4-web\src`、`index.html`、tests、`requirement.txt`、`server`。

---

## 1. 双 exe 重建（p9 §7 精确命令，workdir = v4-web）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 静态快照 + 双 bundle | `node build\bundle-server.mjs` | ✓ exit 0；`static-map.generated.js` 649,825 B（29 文件，内容 634,965 B，较 p9 的 628,086 B 增长 = 引擎/AI 改动已入快照） |
| SEA blob | `node --experimental-sea-config build\sea-lan.json` / `build\sea-single.json` | ✓ 均 `Wrote single executable preparation blob`，exit 0 |
| exe 注入 | `Copy-Item (node -p process.execPath)` + `npx postject … NODE_SEA_BLOB … --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2` | ✓ 双双 `💉 Injection done!`，exit 0 |

**新产物大小**（对比 p9 时点）：

| 产物 | 大小 | p9 时点 |
|---|---|---|
| `dist\oikill-lan.exe` | **93,787,648 B（89.44 MiB）** | 93,774,336 B |
| `dist\oikill-single.exe` | **93,788,160 B（89.44 MiB）** | 93,774,848 B |
| `dist\lan.blob` / `dist\single.blob` | 1,120,818 / 1,121,430 B | 1,107,623 / 1,108,235 B |
| `dist\bundle-lan.cjs` / `dist\bundle-single.cjs` | 1,120,774 / 1,121,383 B | 1,107,579 / 1,108,188 B |

- esbuild 5 条 warning 与 p5/p9 记录完全一致（`core.js` 重复键 `blockTimes`/`usedSeal`、重复 case `skipPlay`/`gift`/`funReport`），为引擎源码既有问题、非本次引入。
- postject `The signature seems corrupted!` ×2 = 预期（Authenticode 签名被注入破坏 → SmartScreen 提示，见 §8）。

## 2. 双 exe 冒烟证据（新脚本 `build\smoke-http.mjs`，Buffer.equals 逐字节比对）

端口 8080 实测空闲，两 exe 均绑定 8080；脚本自起 exe → 解析横幅端口 → HTTP 断言 → stdin `q` → 断言 exit 0。比 p9 的 smoke-exe.ps1 更强：字符串 `-ceq` 换成了 **Node `Buffer.equals` 字节级比对**，并额外断言 core.js 内容含 `duelTurns` 与 `tiebreakExtra`（新平衡规则标记）。

**oikill-lan.exe --no-browser（mode: lan）**：

| 检查 | 结果 |
|---|---|
| GET /api/hello | **PASS** HTTP 200 okJson=true |
| GET / | **PASS** HTTP 200，与磁盘 `index.html` 字节相等（104,066 B） |
| GET /src/data/cards.js | **PASS** HTTP 200，与磁盘字节相等（12,731 B） |
| GET /src/engine/core.js | **PASS** HTTP 200，与磁盘字节相等（**98,567 B**，p9 时点 95,090 B）且 `hasDuelTurns=true` `hasTiebreakExtra=true` |
| clean-exit | **PASS** stdin 'q' → exitCode=0 |
| extract-dir | **PASS** `%TEMP%\oikill-static-29552\root` 29 个文件 |

**oikill-single.exe --no-browser --check-index（mode: single）**：

| 检查 | 结果 |
|---|---|
| GET /api/hello | **PASS** HTTP 200 okJson=true |
| GET / | **PASS** HTTP 200 字节相等（104,066 B） |
| GET /index.html | **PASS** HTTP 200 字节相等（104,066 B） |
| GET /src/data/cards.js | **PASS** HTTP 200 字节相等（12,731 B） |
| GET /src/engine/core.js | **PASS** HTTP 200 字节相等（98,567 B）`hasDuelTurns=true` `hasTiebreakExtra=true` |
| clean-exit | **PASS** exitCode=0 |
| extract-dir | **PASS** `%TEMP%\oikill-static-39816\root` 29 个文件 |

**关键证据**：平衡规则改动（Fix-Balance 的 `duelTurns`/`tiebreakExtra`）位于 `src\engine\core.js`，冒烟中该文件的**逐字节相等 + 规则标记存在**直接证明刷新后的快照（含新平衡规则）已烤入两个 exe。

## 3. Pages 单文件重建与 vm 冒烟

- `node build\bundle-pages.mjs` → exit 0，结构自检通过（`<script src=` 0 个 / 内联 `<script>` 2 个 / UI 内联脚本保留 / createLocalClient 存在）。
- **`dist\oikill-pages.html` = 295,178 B（288.3 KiB）**（p5 时点 293,349 B，增长 = 引擎/AI 改动）；bundle `dist\_pages.js` = 193,231 B（188.7 KiB）。
- 新脚本 `build\pages-smoke.mjs`（镜像 p5 口径：vm 加载 `_pages.js`，`global.window=globalThis`，无 DOM/无服务器），**14/14 全过**：
  - 命名空间 6 项：OIKill 对象、`ui.adapter.createLocalClient`、`ui.fx.createFx`、`sound.sfx.createSfx`、引擎扁平 api（publicView/createGame/drive/timeoutPrompt）、`ai.{difficulty,scorer,identityPolicy,heuristics}`；
  - 对局 8 项：`createLocalClient({thinkMs:0, pid:0, seed:7})` 1 人类 + 5 AI 整局 → setup total=6/pid=0 → gameover（winner=反贼）→ `g.over===true` → **终局 120 牌守恒** → 提示残留=0 → 非法动作=0 → 事件流 states=64/prompts=7/humanTurns=10/rounds=10。
  - exit 0，`=== PAGES SMOKE ALL PASS ===`。

## 4. 发布目录全量刷新（先清空内容、保留目录，再按清单复制）

**GithubServerVer\**（清空后复制，根 9 文件 / src 28 / server 3 / docs 45 / build 5，无 node_modules、无 dist、无 static-map.generated.js）：

- 整目录：`src\`（ai/data/engine/net/sound/ui 全部）、`server\`（boot.js、lan-server.js、single-server.js）、`docs\`（45 文件）；
- 根文件：`game.js`、`index.html`、`test.js`、`test-extra.js`、`suite-d.js`、`README.md`、`bugreport.md`、`package.json`、`package-lock.json`；
- `build\` 仅 5 个构建脚本：`bundle-server.mjs`、`emit-static-map.mjs`、`sea-lan.json`、`sea-single.json`、`single-e2e.mjs`；
- 排除：`node_modules\`、`dist\`、`build\static-map.generated.js`、其余 build 诊断/烟测脚本与 `_*.txt` 存档、`test\`（按任务清单未列）。

**UserVer\**（清空后复制，根 8 文件 / src 28 / server 2 / build 5）：

- `src\`：仅 `data`、`engine`、`ai`、`net`、`ui`、`sound` 六个子目录；
- `server\`：仅 `boot.js`、`single-server.js`（按任务清单，**不含 lan-server.js**）；
- 根文件：`game.js`、`index.html`、`test.js`、`test-extra.js`、`suite-d.js`、`README.md`、`package.json`、`package-lock.json`；
- `build\`：与 GithubServerVer 相同的 5 个 bundle 脚本。
- 注记：UserVer 按清单不含 `server\lan-server.js`，因此在其内直接跑 `bundle-server.mjs` 的 lan 入口会报缺文件（single 入口正常）；此为任务清单的既定裁剪，未擅自加文件。

## 5. 更新日志（两份内容完全一致）

`CHANGELOG.md` 与 `更新日志.md` 均在文件顶部（`# OI杀 更新日志` 之后、`## v4.0 —— 全新 Web 版` 之前）新增 **`## v4.0 完成`** 节，要点：双 exe 已构建（≈89.4MiB，Node SEA，目标机无需 Node）；GitHub Pages 已上线 https://leoooooooooisme.github.io/OI-kill/ ；平衡终版表（easy 32.0/59.0/9.0 三指标全达标、normal 38.6/51.0/10.4 主公方差 1.0pp 抽样噪声级、hard 41.0/44.5/14.5）；平衡规则微调两条（单挑回血每 2 回合 1 点、平票再战一轮→判主公方，用户裁决 A，已回写需求 2.6④/2.8/FAQ#15）；引擎终局过渡态清扫 + 提示清零修复；符合性 runbook 187 条（107 自动化/52 手动/22 裁定/6 未覆盖 = 扩展包与 Solo 模式）；测试全绿统计（A40/B5/C6/E34/D8/self-test62/transport74/room73/adapter67）；已知限制（未签名 SmartScreen、浏览器端 AI 回落旧启发、Solo 教学模式与扩展包未实现、LAN 需放行防火墙）。

## 6. 终验门禁（9 项，全绿，exit 0）

```
node test.js
=== 汇总: A通过=40/40 (不含120守恒=40/40) | B通过=5/5 | C通过=6/6 | 耗时=4.9s ===

node test-extra.js
=== 汇总: E通过=34/34 | 断言失败=0 ===

node suite-d.js
=== 汇总: D通过=8/8 | 断言=171 | 断言失败=0 | 耗时=0.2s ===

node src\ai\self-test.js
=== 汇总: 通过 62 / 失败 0 ===

node src\net\transport-test.js
结果: 74 断言通过, 0 失败

node src\net\room-test.js
room-test 汇总: 通过 73/73  失败 0

node src\ui\adapter-test.js
adapter-test 汇总: 通过 67/67  失败 0

node test\ai-stats.mjs 100 normal --no-human --quiet
========== 汇总: 100 局 @ normal [全AI] (种子 1..100) ==========
  胜方阵营: 反贼 43.0% | 主公方 47.0% | 内奸(摸鱼怪) 10.0%
  平均回合数: 15.9 | 平均行动轮数: 54.2
  崩溃=0 非法动作局数累加=0 守恒失败=0 终局提示残留=0(信息项) cap=0
  健康检查: 全部通过 ✓
```

## 7. 提交

- `git add -A` 后经 `git diff --cached --name-only` 校验：**暂存区 124 文件，无 `dist\`、无 `node_modules`**（根 `.gitignore` 的 `node_modules/`、`dist/` 两条覆盖，`static-map.generated.js` 属 build 源码、随 build\ 入库）。
- 提交：**`70f9d0f`** `feat(v4): P10平衡规则+符合性runbook+终验重建(双exe刷新+GithubServerVer/UserVer同步+更新日志)`，124 files changed, 17057 insertions(+), 2013 deletions(-)。**未 push**（按要求）。
- 本报告以独立 docs 提交随后入库（避免把提交哈希写进提交自身）。

## 8. 失败与注意事项

- **失败项：无。** 全部步骤一次通过。
- 过程插曲（已解决，非交付物问题）：自写 `build\smoke-http.mjs` 首版在退出时序上触发 libuv `UV_HANDLE_CLOSING` 断言（exit -1073740791）——脚本 teardown 竞态，exe 本身烟测断言已全 PASS；已改为等子进程 stdio 自然排空再退出，重跑 exit 0。
- 已知限制（随 v4.0 发布说明一并记录于更新日志）：exe 未签名（SmartScreen 首次运行选「仍要运行」）；浏览器端 AI 回落旧启发式；Solo 教学模式与扩展包未实现；局域网需放行防火墙（exe 横幅打印 netsh 提示）。
- 后续建议：p9 §6.5 的「快照=构建时刻 src 状态」警告已通过本次重跑流水线消解；今后任何 `v4-web\src` 或 `index.html` 改动后，发布前须重跑 `node build\bundle-server.mjs` + SEA + postject 三步刷新 exe。

（完）
